import{useEffect,useMemo,useState}from'react';import{ChevronRight,MapPin,RefreshCw,X}from'lucide-react';import{request}from'./api';
const LABELS:any={NEW:'RECIBIDO',ACCEPTED:'ACEPTADO',PREPARING:'PREPARANDO',READY:'LISTO',COURIER_REQUESTED:'CADETE SOLICITADO',ASSIGNED:'ASIGNADO',PICKED_UP:'RETIRADO',ON_THE_WAY:'EN CAMINO',DELIVERED:'ENTREGADO',CANCELLED:'CANCELADO'};const money=(n:any,c:string)=>`${c} ${Number(n||0).toLocaleString()}`;
function nextAction(o:any){if(o.status==='NEW')return['Aceptar','ACCEPTED'];if(o.status==='ACCEPTED')return['Preparar','PREPARING'];if(o.status==='PREPARING')return['Marcar listo','READY'];if(o.status==='READY'){if(o.delivery_method==='COURIER')return['Solicitar cadete','COURIER_REQUESTED'];if(o.delivery_method==='PICKUP')return['Entregado','DELIVERED'];return['Despachar','PICKED_UP']}if(o.status==='COURIER_REQUESTED')return['Marcar asignado','ASSIGNED'];if(o.status==='ASSIGNED')return['Marcar retirado','PICKED_UP'];if(o.status==='PICKED_UP')return['En camino','ON_THE_WAY'];if(o.status==='ON_THE_WAY')return['Entregado','DELIVERED'];return null}
export default function OrderManagement(){const[orders,setOrders]=useState<any[]>([]);const[selected,setSelected]=useState<any>(null);const[filter,setFilter]=useState('ALL');const[busy,setBusy]=useState(false);async function load(){const rows=await request('/admin/order-management');setOrders(rows);if(selected){const fresh=rows.find((x:any)=>x.id===selected.id);if(fresh)setSelected(fresh)}}useEffect(()=>{load();const t=setInterval(load,15000);return()=>clearInterval(t)},[]);const rows=useMemo(()=>filter==='ALL'?orders:orders.filter(x=>x.status===filter),[orders,filter]);async function change(o:any,status:string){setBusy(true);try{const r=await request(`/admin/order-management/${o.id}/status`,{method:'PATCH',body:JSON.stringify({status})});setSelected(r);await load()}finally{setBusy(false)}}async function cancel(o:any){const reason=window.prompt('Motivo de cancelación');if(!reason)return;setBusy(true);try{const r=await request(`/admin/order-management/${o.id}/cancel`,{method:'POST',body:JSON.stringify({reason})});setSelected(r);await load()}finally{setBusy(false)}}return <><div className="toolbar"><div><div className="eyebrow">CONTROL CENTRAL DE PEDIDOS</div><h2 style={{margin:'4px 0'}}>Todos los pedidos</h2></div><button className="btn ghost" onClick={load}><RefreshCw size={15}/> Actualizar</button></div><div className="actions" style={{marginBottom:14}}>{['ALL','NEW','ACCEPTED','PREPARING','READY','COURIER_REQUESTED','ASSIGNED','PICKED_UP','ON_THE_WAY','DELIVERED','CANCELLED'].map(s=><button key={s} className={filter===s?'btn gold':'btn ghost'} onClick={()=>setFilter(s)}>{s==='ALL'?'Todos':LABELS[s]}</button>)}</div><div className="table-wrap"><table><thead><tr><th>Pedido</th><th>Comercio</th><th>Cliente</th><th>Estado</th><th>Entrega</th><th>Total</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><b>#{o.id.slice(0,8)}</b><div className="muted">{new Date(o.created_at).toLocaleString()}</div></td><td>{o.business_name}</td><td>{o.customer_name||'Cliente'}<div className="muted">{o.customer_phone||'Sin teléfono'}</div></td><td><span className="badge">{LABELS[o.status]}</span></td><td>{o.delivery_method}</td><td>{money(o.total,o.currency)}</td><td><button className="btn ghost" onClick={()=>setSelected(o)}>Ver detalle <ChevronRight size={14}/></button></td></tr>)}</tbody></table>{!rows.length&&<div className="card muted">No hay pedidos con este filtro.</div>}</div>{selected&&<div className="modal"><div className="modal-card" style={{maxWidth:780,maxHeight:'92vh',overflow:'auto'}}><div className="toolbar"><div><div className="eyebrow">PEDIDO #{selected.id.slice(0,8)}</div><h2>{selected.business_name} · {LABELS[selected.status]}</h2></div><button className="btn ghost" onClick={()=>setSelected(null)}><X size={16}/> Cerrar</button></div><div className="grid" style={{gridTemplateColumns:'repeat(2,minmax(0,1fr))'}}><div className="card"><b>Cliente</b><div>{selected.customer_name||'—'}</div><div className="muted">{selected.customer_phone||'—'}</div></div><div className="card"><b>Pago</b><div>{selected.payment_method}</div><div className="muted">{selected.payment_status}</div></div></div><div className="card"><b>Entrega</b><div>{selected.delivery_method}</div><div>{selected.delivery_address||'Retiro en comercio'}</div><div className="muted">{selected.distance_km??0} km · Envío {money(selected.delivery_fee,selected.currency)}</div>{selected.delivery_latitude!=null&&selected.delivery_longitude!=null&&<a className="btn ghost" target="_blank" rel="noreferrer" href={`https://www.google.com/maps?q=${selected.delivery_latitude},${selected.delivery_longitude}`} style={{display:'inline-flex',marginTop:10}}><MapPin size={15}/> Ver ubicación</a>}</div><div className="card"><b>Productos</b>{selected.items.map((i:any)=><div key={i.id} style={{display:'flex',justifyContent:'space-between',padding:'9px 0',borderBottom:'1px solid #17345D'}}><span>{i.quantity} × {i.name}{i.presentation_label?` · ${i.presentation_label}`:''}</span><b>{money(i.subtotal,selected.currency)}</b></div>)}<div style={{display:'flex',justifyContent:'space-between',marginTop:12}}><b>Total</b><b>{money(selected.total,selected.currency)}</b></div></div>{selected.notes&&<div className="card"><b>Observaciones</b><div>{selected.notes}</div></div>}{selected.cancellation_reason&&<div className="error-card"><b>Cancelado:</b> {selected.cancellation_reason}</div>}<div className="actions">{nextAction(selected)&&<button disabled={busy} className="btn gold" onClick={()=>change(selected,nextAction(selected)![1])}>{nextAction(selected)![0]}</button>}{!['DELIVERED','CANCELLED','ON_THE_WAY'].includes(selected.status)&&<button disabled={busy} className="btn" onClick={()=>cancel(selected)}>Cancelar pedido</button>}</div></div></div>}</>}
