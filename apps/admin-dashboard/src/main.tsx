import React,{useState}from'react';import{createRoot}from'react-dom/client';import App from'./App';import OrderManagement from'./OrderManagement';import'./styles.css';
function Root(){const[orders,setOrders]=useState(false);return <React.StrictMode><App/>{!orders&&<button onClick={()=>setOrders(true)} style={{position:'fixed',right:22,bottom:22,zIndex:9999,border:0,borderRadius:14,padding:'14px 18px',fontWeight:800,cursor:'pointer',background:'#F5C443',color:'#08111F',boxShadow:'0 12px 30px #0008'}}>📦 Control completo de pedidos</button>}{orders&&<div style={{position:'fixed',inset:0,zIndex:9998,background:'#020611',overflow:'auto',padding:24}}><div style={{maxWidth:1320,margin:'0 auto'}}><button className="btn ghost" onClick={()=>setOrders(false)} style={{marginBottom:16}}>← Volver al panel</button><OrderManagement/></div></div>}</React.StrictMode>};createRoot(document.getElementById('root')!).render(<Root/>);